{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx truffle test",
    "compile": "npx truffle compile",
    "deploy:bsctest": "npx truffle migrate --f 1 --to 1 --network bsctest",    
    "upgrade:bsctest": "npx truffle migrate --f 2 --to 2 --network bsctest",
    "deploy:sandverce": "npx truffle migrate --f 1 --to 1 --network sandverce",    
    "upgrade:sandverce": "npx truffle migrate --f 2 --to 2 --network sandverce",
    "deploy:nostestnet": "npx truffle migrate --f 1 --to 1 --network nostestnet",    
    "upgrade:nostestnet": "npx truffle migrate --f 2 --to 2 --network nostestnet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0",
    "@openzeppelin/contracts-upgradeable": "^4.8.2",
    "truffle": "^5.8.1"
  },
  "devDependencies": {
    "@openzeppelin/truffle-upgrades": "^1.17.1",
    "@truffle/hdwallet-provider": "^2.1.9",
    "dotenv": "^16.0.3"
  }
}
